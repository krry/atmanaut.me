{
  "dependencies": {
    "autoprefixer": "^9.4.7",
    "hugo-cli": "^0.10.0",
    "postcss": "^7.0.14",
    "postcss-cli": "^6.1.1"
  },
  "scripts": {
    "start": "hugo server -w && open http://localhost:1313",
    "build": "rm -rf ./public && hugo",
    "deploy": "npm run build && now public",
    "release": "npm run deploy && now alias"
  },
  "postcss": {
    "plugins": {
      "autoprefixer": {}
    }
  }
}
